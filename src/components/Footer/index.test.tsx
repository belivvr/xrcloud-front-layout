import { render } from '@testing-library/react'

import React from 'react'

import Footer from '.'

describe('<Footer/>', () => {
  it('renders Footer Component', () => {
    render(<Footer/>)
  })
})
